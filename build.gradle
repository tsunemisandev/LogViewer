subprojects {
    apply plugin: 'java'

    // Compile targeting Java 17 bytecode using whatever JDK is installed (17+)
    tasks.withType(JavaCompile).configureEach {
        options.release = 17
        options.encoding = 'UTF-8'
    }
}
